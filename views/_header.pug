header.header
  nav.nav.nav--tours
    a.nav__el(href='/') 형준투어
  .header__logo
    img(src='/img/logo-white.png' alt='Natours logo')
  nav.nav.nav--user 
    if user
      a.nav__el.nav__el--logout 로그아웃
      a.nav__el(href='/me')
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`photo of ${user.name}`)
        span= user.name
    else
      a.nav__el(href='/login') 로그인
      a.nav__el.nav__el--cta(href='#') 회원가입(아직구현중)

       